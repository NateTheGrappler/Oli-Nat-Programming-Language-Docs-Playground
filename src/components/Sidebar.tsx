import type { Page } from '../config/sections';

function Sidebar({pages}: {pages: Page[]})
{

    return (
        <div>
            <h1>This is the SideBar</h1>
        </div>
    );
}

export default Sidebar;